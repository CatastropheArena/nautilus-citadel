# 示例.env文件
# 复制此文件为.env并修改以下值

# 必填项
KEY_PAIR=KEY_PAIR

# 可选项(如果不设置将使用默认值)
REGION=us-east-1
AMI_ID=ami-085ad6ae776d8f09c
API_ENV_VAR_NAME=API_KEY
EC2_INSTANCE_NAME=my-enclave
INSTANCE_TYPE=m5.xlarge  # ARM64架构的实例类型
# INSTANCE_ID=i-0123456789abcdef0  # 如果设置，将使用现有实例而不是创建新实例

# VPC配置(可选)
# VPC_ID=  # 如果设置，则使用指定的VPC，否则自动选择已有VPC或创建新VPC

# 其他可选配置
# USE_SECRET=y  # 是否使用密钥(y/n)
# SECRET_CHOICE=new  # 创建新密钥或使用现有密钥(new/existing)
USER_SECRET_NAME=API_KEY  # 新密钥名称
SECRET_VALUE=SECRET_VALUE  # 密钥值